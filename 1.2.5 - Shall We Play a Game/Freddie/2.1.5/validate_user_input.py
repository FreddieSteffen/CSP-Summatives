temp = input("Enter the Modulus:")
while (not temp.isdigit()):
  temp = input("With only numbers enter the Modulus:")
mod_value = int(temp)

#is.alpha() checks if it is all letters reterns true if is
